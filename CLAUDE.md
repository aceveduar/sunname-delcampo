# CLAUDE.md — Sunname ERP

> Sistema de gestión de negocio (ERP ligero + POS) **multi-vertical**, pensado para ser **modular, escalable, replicable** a cualquier tipo de negocio **de productos** (ferretería, comercio, abarrotes, etc.) y con una identidad visual propia, premium y genérica.

---

## 0. Cómo usar este archivo

- Este es el **manual operativo del proyecto** para Claude Code. Es la fuente de verdad para _cómo_ trabajamos, _qué_ construimos y _qué reglas_ seguimos.
- **Mantenlo actualizado**: tras cada decisión importante (stack, arquitectura, módulo nuevo, cambio de alcance), actualiza la sección correspondiente y agrega una línea en el §15 (Registro de decisiones). No esperes a que se te pida.
- Mantén este archivo **conciso**. El detalle largo (especificaciones de módulos, casos de uso por vertical) va en `docs/`, no aquí.
- README.md llegará después: documentación para humanos (instalación, uso). Este archivo es para el agente.

---

## 1. Visión del producto

Construir un sistema de **gestión de negocio** (CRM + Compras + Inventario + Facturación + Caja, con más módulos a futuro) que sirva para **cualquier tipo de negocio de productos**: ferretería, comercio al detalle, abarrotes, etc. No es un producto vertical (ya no está atado a "tienda a granel de chiles y moles"): ese caso de uso pasa a ser **un módulo/plantilla más**, no el corazón del sistema. Negocios de **servicios** (consultorio dental/médico, citas, órdenes de trabajo) quedan **fuera de alcance** — decisión 2026-08-20, se resuelven en un sistema aparte, no dentro de Sunname ERP.

- **Construido a la medida, deliberadamente independiente de Odoo**: el objetivo explícito es tener una alternativa propia para ofrecer a negocios que no quieren Odoo (o algo similar), no una extensión de Odoo. Es una decisión de producto tomada a propósito, no por desconocimiento de la alternativa.

- **Multi-vertical**: el núcleo (usuarios, permisos, catálogo de productos/servicios, movimientos de caja, inventario, CRM) debe ser genérico. Lo específico de cada giro (venta a granel por peso, citas médicas, órdenes de trabajo de taller, etc.) se resuelve como **módulos o configuraciones activables**, no como lógica hardcodeada en el núcleo.
- **Replicable / multi-tenant**: lo que se construye debe poder desplegarse para distintos negocios con mínima configuración (visión SaaS).
- **Escalable**: desde un negocio chico de un solo local y sin facturación, hasta uno con múltiples locales, pagos electrónicos y facturación fiscal.
- **Modular**: cada módulo (caja, inventario, CRM, compras, facturación, reportes) debe ser activable/desactivable de forma independiente.
- **Premium pero simple de usar**: nivel de acabado visual y de UX comparable a los sistemas líderes del mercado, sin sacrificar velocidad ni claridad.

---

## 2. Conectividad: online-first, no offline-first (decisión revisada)

**Cambio respecto a la versión anterior de este documento.** La versión original definía el proyecto como _offline-first_. Se redefine así:

- El sistema se diseña **online-first**: se asume que la inmensa mayoría de los negocios objetivo (ferretería, consultorio, comercio, abarrotes) tienen acceso a internet razonablemente estable, igual que asumen la mayoría de los POS comerciales modernos (Square, Clover, el propio Odoo POS, etc.).
- Esto **simplifica muchísimo la arquitectura**: no se necesita un motor de sincronización bidireccional complejo (PowerSync/ElectricSQL) ni resolución de conflictos multi-dispositivo como requisito base. Se reduce tiempo de desarrollo, superficie de bugs y costo de infraestructura.
- Es además más **coherente con la Fase 4 (facturación CFDI 4.0)**: timbrar ante un PAC ya requiere internet en tiempo real, así que un sistema 100% offline-first terminaría necesitando online de todos modos para esa pieza.
- **Pero** no se recomienda ir a "online-only" sin ningún colchón: los cortes de conexión existen (ISP local, corte eléctrico, WiFi del negocio) y en el módulo de **Caja** perder la capacidad de cobrar durante un corte sí es costoso para el negocio.
- **Recomendación concreta**: arquitectura **online-first con resiliencia local acotada** — solo las operaciones críticas de venta/caja se encolan localmente (IndexedDB/local storage) si se pierde la conexión, y se sincronizan automáticamente al recuperarla. El resto del sistema (CRM, reportes, compras, facturación, configuración) puede requerir conexión sin problema. Esto evita construir un sistema offline-first completo (caro, complejo, propenso a bugs de sincronización) mientras se conserva lo único que realmente importa: que la caja no se caiga por un corte de internet de 10 minutos.

> Respuesta directa a tu pregunta: sí, para un sistema pensado para muchos negocios distintos, **online-first es la mejor opción**. Offline-first total solo se justificaría si el público objetivo tuviera conectividad crónicamente mala (zonas rurales sin cobertura, por ejemplo), que no es el caso que describes.

---

## 3. Módulos (alcance funcional)

Módulos previstos desde ahora, con posibilidad de agregar más a futuro sin romper el núcleo:

- **CRM** — clientes, contactos, historial de interacción/compra.
- **Compras** — proveedores, órdenes de compra, recepción de mercancía.
- **Inventario** — existencias, movimientos, múltiples unidades de medida, alertas de stock.
- **Facturación** — cotizaciones, notas de venta, factura fiscal (CFDI 4.0 en fase avanzada).
- **Caja** — apertura/cierre de caja, cobros, métodos de pago, arqueo.
- _(Futuro, no bloqueante para el MVP)_: contabilidad, citas/agenda (útil para dentista/doctor), órdenes de trabajo/servicio, reportes avanzados, multi-almacén, ~~**fotos de producto en Catálogo**~~ (implementado 2026-08-20: bucket público `product-images` en Supabase Storage + columna `products.image_url`; solo owner/local_admin pueden subir/editar/borrar; miniatura visible en la tabla de Catálogo y en los resultados de búsqueda de Caja, que era justo el caso de uso -- distinguir variantes parecidas). Ampliado el mismo día: vista de tarjetas en Catálogo (toggle tabla/tarjetas, `ProductsTab.tsx`) para navegar viendo la foto más grande -- deliberadamente NO se llevó a Caja, ahí importa la velocidad del cajero, no la navegación visual. Toda foto se comprime en el navegador antes de subirse (`lib/image.ts`, máx. 1200px / JPEG calidad 0.82, con fallback a subir el original si el navegador no puede decodificar el formato) -- verificado real: 12.96MB de entrada bajaron a 134KB. Necesario porque el plan gratis de Supabase Storage es de 1GB y las primeras 3 fotos subidas sin comprimir ya pesaban 7.8MB entre las tres., **IA** (ver §14 — módulo opcional, no forma parte del núcleo).

Cada módulo debe poder activarse o desactivarse por negocio/tenant, y el núcleo no debe asumir que todos los módulos están presentes.

---

## 4. Alcance y fases

> Regla: **MVP primero, luego capas.** No se empieza contabilidad ni facturación fiscal hasta que el flujo base (catálogo + caja + inventario) esté sólido en un solo vertical piloto.

- **Fase 0 — Decisiones y setup**: resolver §13, definir stack, crear repo, estructura base, identidad visual genérica.
- **Fase 1 — MVP**: un solo local · catálogo de productos/servicios genérico · caja básica (con resiliencia local ante cortes) · inventario básico · reportes básicos.
- **Fase 2 — Operación**: roles/usuarios · códigos de barras · CRM básico · compras básicas.
- **Fase 3 — Crecimiento**: multi-local · pagos con tarjeta/transferencia · contabilidad · módulos específicos por vertical (venta a granel, agenda de citas, etc.).
- **Fase 4 — Avanzado**: facturación **CFDI 4.0** (vía PAC) · multi-empresa / modo SaaS completo para replicar a otros negocios · marketplace de módulos por vertical.

---

## 5. Requisitos no funcionales (restricciones de arquitectura)

- **Online-first con resiliencia acotada**: ver §2. Solo caja necesita cola local ante cortes de conexión.
- **Multi-tenant desde el diseño**: aislamiento de datos por negocio, aunque la fase 1 solo tenga un tenant real.
- **Multi-dispositivo**: PC del negocio como dispositivo principal, móvil para consulta y tareas ligeras del dueño/encargado. **Diseño responsivo obligatorio.**
- **Gestión de usuarios y accesos fácil**: alta de usuarios, roles y permisos debe poder hacerse desde la UI, sin intervención técnica, desde el día uno.
- **Rápido**, con **UI moderna, premium**, sin sobrecarga visual. Prioridad: claridad y velocidad de uso.
- **Seguridad seria** desde el día uno (autenticación, control de acceso por rol/módulo, manejo de datos sensibles — especial cuidado en verticales como salud, donde puede haber datos personales sensibles).
- **Sin deuda técnica**: resolver pronto lo que costaría caro después. Sin código muerto, sin duplicación, sin redundancia.
- **Arquitectura preparada para crecer**: agregar un módulo o un vertical nuevo no debe implicar reescribir el núcleo.

### 5.1 Modelo de licenciamiento / planes

- **Suscripción mensual por negocio (tenant), no por usuario.** En caja/POS lo relevante es el negocio y sus locales, no el número de personas que lo usan; cobrar por asiento penaliza dar acceso a más empleados.
- **Planes por paquete de módulos** (no à la carte por ahora): p. ej. _Básico_ (Caja + Inventario), _Profesional_ (+ CRM + Compras), _Avanzado_ (+ Facturación/CFDI). El à la carte se evalúa más adelante, cuando haya varios tenants y datos reales de qué módulos usa cada quién.
- **Sin cobro por transacción/uso** por ahora — genera fricción en negocios pequeños y es difícil de calibrar sin datos de uso reales.
- **Para el MVP (un solo tenant piloto)**: no formalizar tiers todavía. Tarifa fija simple para el piloto; los planes reales se definen con el segundo o tercer cliente.

---

## 6. Roles y permisos (propuesta a validar)

| Rol                           | Para qué                   | Accesos típicos                                                                      |
| ----------------------------- | -------------------------- | ------------------------------------------------------------------------------------ |
| **Propietario / Super admin** | El dueño                   | Todo: configuración, usuarios, precios, reportes financieros, multi-local            |
| **Administrador de local**    | Encargado de una sucursal  | Operación de su local, inventario, reportes de su local, gestión de cajeros          |
| **Cajero / Vendedor**         | Operación diaria           | Vender, abrir/cerrar caja, consultar productos. Sin acceso a costos ni configuración |
| **Contador** (Fase 3+)        | Tareas fiscales/contables  | Reportes contables, facturación. Solo lectura sobre operación                        |
| **Consulta (dueño en móvil)** | Ver el negocio desde fuera | Dashboards, reportes, ventas en vivo. Solo lectura + acciones puntuales              |

> Recomendación: permisos **granulares por módulo** (no solo por rol), para que el modelo escale a distintos tipos de negocio sin rediseñar el RBAC cada vez.

---

## 7. Identidad visual

- **Nombre del producto: Sunname ERP.**
- **Paleta de colores — definida**: dos colores de identidad + neutrales + dos semánticos, sin exceso de color:
  - **Índigo profundo** `#1E2A47` — primario (header, sidebar, botones primarios, texto de marca).
  - **Dorado apagado** `#C9A227` — único acento (CTAs, totales, estados activos). Se usa con moderación.
  - **Grafito** `#2B2B2E` — texto principal.
  - **Gris cálido claro** `#F1EFEA` — fondo.
  - **Verde salvia** `#3B7A57` — éxito/confirmación (semántico, apagado, no verde de semáforo).
  - **Terracota** `#B23A34` — alerta/error (semántico, apagado).
- **Sin emojis** en ningún lado (UI, código, documentos). Set de iconos consistente — recomendación: **Lucide** (o Phosphor / Tabler).
- Estética: moderna, atractiva, sobria. Menos es más.
- Tipografía: pendiente de elegir (Fase 0), coherente con la paleta anterior.

**Identidad del producto vs. branding del tenant — principio de diseño:**

- El **chrome del sistema** (dashboard, caja, inventario, menús) usa siempre la identidad de **Sunname ERP** (índigo/dorado), sin importar el negocio que lo use — igual que Odoo se ve como Odoo o Square se ve como Square, independientemente de quién lo opere.
- El **branding de cada negocio/tenant** (p. ej. el verde de la marca de la tienda de chiles/moles) vive únicamente en lo que ven los **clientes finales de ese negocio**: ticket impreso, etiquetas, encabezado del recibo — nunca en la interfaz interna del sistema.
- **Regla de implementación desde el día uno**: ningún color va hardcodeado en el código de UI — todo pasa por variables/tokens de tema. Esto no cuesta más ahora y deja la puerta abierta a que el logo/color del tenant en tickets/etiquetas sea configurable por negocio en el futuro, sin tener que repintar el sistema completo por cliente.

---

## 8. Stack tecnológico — CONFIRMADO (con una tensión a vigilar)

**Confirmado:**

- **TypeScript** en todo el proyecto · monorepo.
- **UI**: React + Vite + Tailwind + componentes accesibles (shadcn/ui sobre Base UI) · iconos Lucide · react-router-dom.
- **Aplicación**: **web app estándar** (no requiere shell nativo tipo Tauri como base, al dejar de ser offline-first). Servida como **PWA responsiva** para PC y móvil.
- **Resiliencia de caja**: cola local ligera (IndexedDB) solo para operaciones de venta/caja, con reintento automático de sincronización — no un motor de sync completo para todo el sistema.
- **Hardware por vertical (opcional)**: para verticales que necesiten báscula, impresora de etiquetas u otro periférico (p. ej. venta a granel), se contempla un **módulo nativo opcional** (Tauri) que se activa solo si ese vertical lo requiere. No es parte del núcleo.

**Backend y multi-tenant — decidido, con una tensión que hay que vigilar:**

- **Multi-tenant: una base de datos por negocio** (no esquema/RLS compartido). Es la opción más aislada y simple de razonar (un negocio nunca puede ver ni por error los datos de otro), y es la preferencia explícita del proyecto.
- **Backend gestionado: Supabase** (Postgres + Auth) para no operar infraestructura desde el día uno.
- **La tensión a vigilar**: el modelo "una base de datos por negocio" y "Supabase" no son gratis de combinar a escala. Supabase está pensado como _un proyecto = una instancia de Postgres_; si cada negocio-cliente tiene su propia base, cada uno implica su propio proyecto de Supabase con su propio costo mensual (aprox. desde ~$25 USD/mes en el plan Pro por proyecto, sujeto a cambios — verificar precio vigente). Para el vertical piloto (un solo tenant) esto no importa nada. Pero si el modelo de negocio es vender a **muchos negocios pequeños con cuota modesta**, el costo de una instancia dedicada por cliente puede no ser rentable frente a una arquitectura de base compartida con RLS.
- **Recomendación práctica**: arrancar con base de datos por negocio (como se decidió) para el piloto y los primeros clientes, pero **diseñar el esquema desde ya compatible con una migración a RLS compartido** (mismo modelo de datos, mismas migraciones) por si el costo por-tenant se vuelve un problema al crecer. No es necesario decidirlo hoy — es una revisión a hacer en Fase 3 con datos reales de cuántos clientes y a qué precio.
- **Nota aparte sobre Supabase**: no hay región de Supabase en México; las más cercanas suelen ser en EE. UU. o Brasil (verificar disponibilidad vigente). Para un POS esto no suele ser un problema de latencia perceptible, pero vale la pena probarlo con datos reales antes de comprometerse del todo.

---

## 9. Hardware (opcional, por vertical)

El sistema **no asume hardware específico** en el núcleo. Cada vertical puede requerir periféricos distintos:

- **Báscula con interfaz** (serie/USB): relevante solo para negocios con venta a granel/por peso.
- ~~**Lector de código de barras**~~: relevante para inventario/retail en general — la mayoría funciona como teclado (HID), integración trivial (ya construida en Caja, escanear = escribir + Enter). Resuelto 2026-08-31: para empezar, uno genérico USB 2D (Netum/Tera/Symcode, ~$350-800 MXN) -- el catálogo apenas tiene 1-2 productos con código real, no se justifica algo más caro todavía. Si el uso real lo justifica más adelante, subir a un Honeywell Voyager 1200g (~$1,189-1,900 MXN, IP42, 5 años de garantía).
- **Impresora de etiquetas térmica**: solo si el negocio la necesita -- hoy la impresión de etiquetas (§3) funciona con cualquier impresora normal, no requiere una térmica dedicada.
- **Terminal de pago con tarjeta**: relevante en fases posteriores para varios verticales.

---

## 10. Reglas de trabajo para Claude Code (cómo debes comportarte)

- Actúa como **arquitecto de software senior**, **diseñador UX/UI experto** y **redactor**.
- **Guía honesta, no complacencia**: si se pide algo que daña el sistema, dilo y propón mejor camino. No des la razón si no la hay. Si algo va bien, dilo también.
- Antes de construir algo grande, **contextualiza y propón** (decisiones, trade-offs) en vez de ejecutar a ciegas.
- **Referencia de clase mundial**: Sunname ERP tiene identidad visual propia (§7) y no es un clon de nadie, pero al diseñar un flujo (caja, catálogo, inventario, etc.) vale la pena mirar qué resuelven bien sistemas líderes (Square, Toast, Clover, Shopify POS, Odoo) — patrones de UX, atajos, validaciones — y adaptar lo que aplique a Sunname, no reinventar desde cero lo que el mercado ya resolvió bien. La identidad de marca es de Sunname; los buenos patrones de producto pueden venir de donde sea.
- **Cero deuda técnica deliberada**: nada de "lo arreglo después". Sin código muerto, duplicado ni redundante.
- **Mejores prácticas** de arquitectura, seguridad y código limpio en cada commit.
- **Sin emojis**. Iconos para la UI.
- **Git/GitHub** para todo el desarrollo (ver §11).
- **Mantén este `CLAUDE.md` actualizado** tras cambios importantes y registra la decisión en §15.
- Avisa si se necesita una **skill o herramienta** instalada/configurada en Claude Code antes de avanzar.

---

## 11. Estándares de código y flujo de trabajo (se concreta al fijar el stack)

- **Git/GitHub**: ramas por feature, commits descriptivos (Conventional Commits recomendado), PRs revisables. Definir estrategia de ramas.
- **Calidad**: linter + formateador + types estrictos · pruebas para la lógica crítica (caja, inventario, permisos).
- **Estructura**: modular por dominio (no por tipo de archivo), para que cada módulo (caja, inventario, CRM, compras, facturación) sea aislable y activable de forma independiente.

**Modularidad "fácil de integrar y quitar" — cómo se logra sin sobre-construir:**

- **Límite de módulo estricto**: cada módulo vive en su propia carpeta/paquete, con su propio modelo de datos y su propia API interna. Un módulo **nunca** importa directamente las tablas o funciones internas de otro — si necesita algo de otro módulo, pasa por una interfaz/evento definido explícitamente. Esta disciplina es la que realmente hace que un módulo se pueda quitar sin romper los demás; es barata de mantener si se respeta desde el commit uno, y muy cara de arreglar después si no.
- **Registro de módulos por tenant**: una tabla de configuración simple (`tenant_modules`: qué módulos tiene activos cada negocio) controla qué se muestra/permite por negocio. Esto ya estaba implícito en el modelo de licenciamiento por paquetes (§5.1) — es el mismo mecanismo.
- **Feature flags** dentro de un módulo para funcionalidades más finas (p. ej. "venta a granel" dentro de Inventario), sin necesidad de que sea un módulo separado.
- **Lo que NO conviene construir todavía**: un sistema de plugins de verdad (SDK para que terceros escriban módulos, carga dinámica de código en tiempo de ejecución, marketplace de instalación) — eso es lo que a Odoo le tomó años madurar, y construirlo antes de tener varios verticales reales usándolo es resolver un problema que aún no tienes con datos. "Fácil de integrar y quitar" para el MVP significa: módulos con límites limpios que se **activan/desactivan por configuración**, no un runtime de plugins de terceros. Esa pieza se revisita cuando haya evidencia real de qué funcionalidades pide la gente y con qué frecuencia.

- **Seguridad**: validación de entradas, manejo de secretos fuera del repo, control de acceso por rol/módulo, registro de auditoría en operaciones sensibles (caja, precios, usuarios).

---

## 12. Antes de empezar — checklist de prerrequisitos

- [x] Definir **nombre del proyecto/producto**: **Sunname ERP**.
- [x] Definir paleta de colores (§7). Tipografía aún pendiente (Fase 0).
- [ ] Cuenta y **repositorio en GitHub** creados.
- [ ] Decidir los puntos del §13.
- [x] **Vertical piloto**: **Del Campo** — tienda de chiles, moles y semillas.
- [ ] (Fase 1) Catálogo real de productos del vertical piloto, con precios y unidades.
- [ ] (Si aplica al vertical piloto) Modelo de hardware necesario y su documentación.
- [ ] (Fase 4) Datos fiscales + contrato con un **PAC** para CFDI 4.0.
- [ ] Confirmar **skills/herramientas** que Claude Code necesite (se define al fijar stack).

---

## 13. Decisiones pendientes (resolver ANTES de codear)

1. **¿Construir desde cero o sobre una base existente?** — Resuelto: **a la medida, deliberadamente independiente de Odoo**. El sistema se piensa como una alternativa propia a ofrecer a negocios que no quieren Odoo.
2. **Stack tecnológico** (§8): confirmado.
3. **Nube/backend**: Supabase, con la tensión de costo por-tenant anotada en §8 a revisar en Fase 3.
4. **Multi-tenant**: decidido — **una base de datos por negocio**, con nota de diseño para poder migrar a esquema compartido si el costo por-tenant se vuelve un problema al crecer (§8).

> ~~¿Offline-first vs online-first?~~ — Resuelto en §2: **online-first con resiliencia acotada en caja**.
> ~~Vertical piloto para el MVP?~~ — Resuelto: **Del Campo**, tienda de chiles, moles y semillas.
> ~~Modelo de licenciamiento/planes?~~ — Resuelto en §5.1: **suscripción por negocio (tenant), no por usuario**, sin formalizar tiers todavía (un solo tenant piloto por ahora).
> ~~Nombre del producto y paleta de colores?~~ — Resuelto en §7: **Sunname ERP**, paleta índigo profundo + dorado apagado + neutrales + semánticos.

**No queda ninguna decisión abierta en esta lista.** Con esto, §13 está formalmente cerrado — se puede pasar a Fase 0 (setup) del §4.

**Deliberadamente pospuesto, no pendiente de decidir ahora:**

- ~~**Modelo de datos de "servicios"**~~ — Decidido 2026-08-20: un negocio de servicios (ej. consultorio dental con citas) **no se va a resolver dentro de Sunname ERP**, el usuario planea construirlo como un sistema aparte. Sunname ERP se queda enfocado en negocios de productos (retail, abarrotes, ferretería, etc.) -- ya no es "pospuesto", es fuera de alcance.
- **Campos configurables por vertical**: no se define en abstracto con un solo vertical. Se resuelve con evidencia real cuando se incorpore el segundo vertical, no antes.

---

## 14. IA responsable y consideraciones operativas adicionales

### 14.1 IA — criterio de inclusión, no lista de funciones

- **IA es un módulo opcional más**, activable/desactivable por tenant igual que los demás (§11) — no vive dentro del núcleo ni se mete en flujos que ya funcionan bien sin ella.
- **Regla de inclusión**: una funcionalidad de IA entra al roadmap solo si resuelve un problema real y concreto mejor de lo que se resuelve sin IA — nunca porque "hay que tener IA". Si un cálculo es determinista y ya funciona (p. ej. peso × precio = importe), no necesita IA encima; eso sería complejidad sin beneficio.
- **Candidatos con valor real, a evaluar cuando el núcleo esté sólido** (no antes, y no todos a la vez — elegir uno, medir si de verdad ahorra tiempo, luego el siguiente):
  - Captura de facturas/tickets de proveedor por foto → llenar Compras automáticamente (ahorra captura manual).
  - Sugerencias de reorden en Inventario basadas en patrones de venta/estacionalidad.
  - Búsqueda/reportes en lenguaje natural ("¿cuánto vendí de mole en abril?").
  - Detección de anomalías en arqueos de Caja (posibles errores de captura o mermas fuera de rango).
- **Explícitamente fuera por ahora**: chatbot genérico, asistente conversacional de propósito general, o cualquier feature de IA que se agregue solo por tendencia de mercado.

### 14.2 Otras cosas a resolver antes de que duelan (no bloquean Fase 0, pero conviene tenerlas en el radar)

- **Respaldo y recuperación por tenant**: con una base de datos por negocio (§8), definir desde Fase 1 una política simple de backups automáticos y cómo se restauraría un negocio si su base se corrompe.
- ~~**Observabilidad**~~ — Resuelto 2026-08-20: Sentry conectado (`apps/web/src/lib/sentry.ts`), con captura automática de errores no manejados (`Sentry.ErrorBoundary` envolviendo la app) y captura manual explícita en las rutas donde un fallo silencioso de verdad duele — abrir/cerrar caja y registrar una venta (`src/lib/errors.ts` → `reportError`). El resto de los `toast.error(...)` sueltos por los demás módulos (Catálogo, Inventario, Compras, CRM, Usuarios) todavía no reportan a Sentry, solo muestran el toast — pendiente si se quiere ampliar la cobertura.
- **Entornos y despliegue**: separar desarrollo/producción y tener un proceso de despliegue repetible desde el principio, aunque sea simple — evita que "probar algo nuevo" ponga en riesgo al tenant piloto que ya está operando.
- **Actualizaciones sin romper tenants en vivo**: cómo se despliega una migración de base de datos o un cambio de módulo cuando ya hay negocios reales usando el sistema (no es un problema del MVP con un solo tenant, pero si no se piensa la estrategia de migraciones desde ahora, se vuelve doloroso con el segundo o tercer cliente).
- ~~**Aviso de privacidad y términos de servicio**~~ — Primer borrador creado 2026-08-20: `docs/aviso-privacidad.md` y `docs/terminos-servicio.md`. Cubren la relación Sunname↔Negocio-cliente (no la relación Del Campo↔sus propios clientes, que es responsabilidad de cada Negocio). Tienen placeholders sin llenar (razón social, domicilio, cláusulas de responsabilidad/terminación) y están explícitamente marcados como pendientes de revisión legal real antes de usarse formalmente — sigue siendo más urgente en cuanto se sume un vertical de salud (dentista/doctor).
- **CFDI 4.0 no es solo "conectar un PAC"**: incluye catálogos del SAT (claves de producto/servicio, unidades, régimen fiscal del emisor y receptor) — vale la pena investigarlo con tiempo antes de llegar a Fase 4, no dejarlo para el final.
- **Capacidad de tiempo real**: este proyecto se construye en paralelo al trabajo en Sunname Partners — conviene ser honesto sobre cuántas horas por semana se le puede dedicar, para que el alcance de cada fase sea alcanzable y no genere frustración por expectativas de tiempo poco realistas.
- ~~**`tenant_modules` no está conectado a la UI**~~ — Resuelto 2026-08-20: nav (`AppShell.tsx`) y rutas (`App.tsx`) ya leen `tenant_modules` vía `useTenantModules()`; Clientes/Compras se ocultan y sus rutas redirigen a `/caja` si el módulo está apagado. Nueva pantalla `/configuracion` (solo owner, coincide con la política RLS `tenant_modules_write`) para prender/apagar CRM, Compras y Facturación. Caja, Catálogo e Inventario quedan fuera del toggle a propósito — son el núcleo del plan "Básico" (§5.1), no módulos opcionales.

---

## 15. Registro de decisiones (ADR ligero)

| Fecha         | Decisión                                                                                                                                                                          | Razón                                                                                                                                  |
| ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| _(pendiente)_ | Pivote de "tienda a granel de chiles/moles" a sistema **multi-vertical**                                                                                                          | Servir a cualquier tipo de negocio (ferretería, dental, comercio, abarrotes, etc.), no solo al caso original                           |
| _(pendiente)_ | **Online-first** en lugar de offline-first, con cola local solo en caja                                                                                                           | La mayoría de los negocios objetivo tiene internet; simplifica arquitectura y es coherente con CFDI (Fase 4), que ya requiere conexión |
| _(pendiente)_ | Identidad visual **genérica**, ya no ligada al negocio de chiles/moles                                                                                                            | El producto ya no es de un solo giro                                                                                                   |
| _(pendiente)_ | Módulos iniciales: CRM, Compras, Inventario, Facturación, Caja                                                                                                                    | Cobertura base común a la mayoría de negocios, con opción de crecer                                                                    |
| _(pendiente)_ | Vertical piloto del MVP: **Del Campo**, tienda de chiles, moles y semillas                                                                                                        | Es el negocio ya conocido a detalle; permite levantar catálogo real rápido                                                             |
| _(pendiente)_ | El sistema se centra primero en **productos**; el modelo de **servicios** se aborda después de terminar el sistema de productos                                                   | Evitar sobre-diseñar el núcleo antes de tener el caso de producto resuelto y probado                                                   |
| _(pendiente)_ | Licenciamiento: **suscripción por negocio (tenant)**, por paquete de módulos, sin tiers formales todavía                                                                          | Simplicidad para el MVP con un solo tenant; evita cobro por usuario o por transacción, que no encajan bien con un POS                  |
| _(pendiente)_ | Sistema **a la medida, independiente de Odoo**                                                                                                                                    | Se ofrece como alternativa propia a negocios que no quieren Odoo                                                                       |
| _(pendiente)_ | Nombre del producto: **Sunname ERP**; paleta: índigo `#1E2A47` + dorado `#C9A227` + neutrales + verde/terracota semánticos                                                        | Identidad genérica, premium, con un solo acento para no sobrecargar la UI                                                              |
| _(pendiente)_ | El vertical piloto (chiles/moles/semillas) usa la identidad de **Sunname ERP**, no la marca del negocio; el branding del negocio queda solo en tickets/etiquetas de cliente final | Separar identidad del producto vs. branding del tenant; evita repintar el sistema por cada cliente                                     |
| _(pendiente)_ | Modularidad vía **límites de módulo + activación por configuración (`tenant_modules`)**, no un runtime de plugins de terceros                                                     | Suficiente para "fácil de integrar y quitar" en el MVP; un SDK de plugins real se evalúa después, con evidencia de varios verticales   |
| _(pendiente)_ | **IA como módulo opcional**, con criterio de inclusión por valor real (no por tendencia)                                                                                          | Evitar IA forzada; se agrega solo cuando resuelve mejor un problema concreto que la alternativa sin IA                                 |
| 2026-08-20    | Primer esquema de base de datos (`supabase/migrations/20260820130031_init_core.sql`): `profiles`+`user_role`, `tenant_modules`, catálogo (`products`, `product_categories`, `units_of_measure`), inventario (`inventory_movements` + vista `inventory_stock`), caja (`cash_sessions`, `sales`, `sale_items`, `sale_payments`, `payment_methods`) | Arranca Fase 1 (catálogo + caja básica + inventario básico) sobre el modelo "una base de datos por negocio" del §8; sin tabla/columna `tenant_id` porque el aislamiento ya lo da la base de datos |
| 2026-08-20    | Permisos vía **RLS de Postgres basada en rol** almacenado en `profiles` (función `current_role_key()`), no roles nativos de Postgres/Supabase                                    | Permite alta de usuarios y roles desde la UI (§5) sin tocar roles a nivel de base de datos; deja lista la base para permisos granulares por módulo (§6) |
| 2026-08-20    | Inventario modelado como **bitácora de movimientos inmutable** (`inventory_movements`, solo insert) + vista `inventory_stock` calculada, sin columna de saldo mantenida a mano    | Evita que el saldo de existencias se desincronice de su propio historial; el costo de recalcular por vista es aceptable al volumen del MVP |
| 2026-08-20    | Toda tabla nueva debe llevar `GRANT` explícito a `authenticated` además de sus políticas de RLS (`supabase/migrations/20260820140358_grant_authenticated_privileges.sql`), y se dejó `ALTER DEFAULT PRIVILEGES` para que las tablas futuras lo hereden solo | RLS y GRANT son capas distintas en Postgres: una tabla creada por migración SQL (a diferencia del Table Editor de Supabase, que sí lo hace solo) no le da privilegios base a `authenticated`, así sus políticas de RLS existan — se detectó al probar el login real (error 42501) |
| 2026-08-20    | shadcn/ui corre sobre **Base UI**, no Radix (corrige el §8 original)                                                                                                                                                              | El CLI actual de shadcn (preset "Nova") trae Base UI por default; es igual de accesible y es lo que ya quedó instalado y funcionando — no vale la pena deshacerlo solo para volver a lo que decía el documento originalmente |
| 2026-08-20    | Tokens de tema separan `--accent` (hover/foco neutro que usan los componentes de shadcn en todas partes) de `--brand-gold` (el dorado de marca, aplicado a mano solo en CTAs/totales/estados activos)                             | Si el dorado ocupara el slot `--accent` de shadcn, aparecería en cada hover de menú/botón ghost, violando "se usa con moderación" del §7 |
| 2026-08-20    | Auditoría de seguridad (`supabase/migrations/20260820173415_security_hardening.sql`): `create_sale` pasa a `security definer` con autorización y precio explícitos adentro; `sales`/`sale_items`/`sale_payments`/`inventory_movements` ya no aceptan insert directo de un cajero (solo owner/local_admin, o vía la función); `handle_new_user` ya no confía en el rol de los metadatos del signup; trigger nuevo evita que cualquiera cambie su propio rol/estado | Se probaron en vivo y se confirmaron explotables antes de corregir: autorregistro público como `owner` sin autenticarse (vía `/auth/v1/signup` con la anon key — cualquiera podía apropiarse el tenant), un cajero podía forjar `unit_price` en `create_sale` para quedarse con la diferencia en efectivo, y podía insertar movimientos de inventario 'out' sin una venta real detrás. Ver detalle completo en el reporte de auditoría de esa fecha |
| 2026-08-20    | Base de datos real limpiada de todo dato de prueba (`supabase/reset-test-data.sql`) y cargado el primer lote real de catálogo de Del Campo (`supabase/seed-real-products.sql`): Ajonjolí Moreno, Cacahuate tostado, Comino entero, Tapioca, Guajillo herradura | Arranque de operación real del piloto; el dueño empezó a dar precios reales de compra/venta |
| 2026-08-20    | **Negocios de servicios quedan fuera de alcance** de Sunname ERP (consultorio dental/médico, citas, órdenes de trabajo) -- se resuelven en un sistema aparte | Decisión explícita del usuario; evita que el núcleo intente servir dos modelos de negocio (productos vs. servicios) a la vez |
| 2026-08-20    | Nav centrada (grid de 3 columnas: logo / nav / usuario) en vez de agrupada a la izquierda -- con pocos módulos activos (crm/purchasing/billing apagados) se veía amontonada a la izquierda con un vacío grande de un solo lado; centrada se ve intencional con 3 módulos o con 8. Nombre de usuario largo ahora trunca con `max-w-48` en vez de desbordar el header | Reportado por el usuario con captura real tras apagar módulos; verificado en vivo con el estado mínimo real de módulos y un nombre largo simulado |
| 2026-08-20    | Caja: el buscador se limpia y recupera el foco solo después de agregar cualquier producto (clic o escaneo) -- antes solo pasaba en el escaneo por código de barras, y el clic dejaba el texto/resultados viejos ahí, comportamiento distinto según cómo se agregó el producto | Una búsqueda = un producto = listo para el siguiente, igual que Square/Toast/Shopify POS; consistencia entre las dos formas de agregar un producto en la pantalla que más se usa del sistema |
| 2026-08-20    | Barra de navegación a todo lo ancho de la pantalla, ya no encerrada en el mismo `max-w-6xl` que el contenido de la página. En monitores anchos (1920px) dejaba 384px de espacio vacío a cada lado -- correcto para texto legible, no para una barra de navegación. El contenido de página (`<main>`) sigue centrado a `max-w-6xl` sin cambios | Reportado por el usuario con una captura real de su pantalla; confirmado con el layout calculado real (no solo por ojo) antes de tocar el código |
| 2026-08-20    | Dorado de marca aplicado a totales (Caja, ticket, "Total vendido" en Reportes) -- CLAUDE.md §7 ya decía "CTAs, totales, estados activos" pero en toda la construcción nunca se había aplicado a ningún total, solo se usaba una vez (diferencia de corte de caja). Con moderación a propósito: no se tocaron botones ni navegación, solo la cifra del total | Revisión de diseño pedida por el usuario ("sistema de alto nivel"); hueco real entre lo documentado y lo construido, no un cambio especulativo |
| 2026-08-20    | Corrección de bug real: `/clientes` y `/compras` redirigían a `/caja` con solo entrar por URL directa o refrescar la página, aunque el módulo estuviera activo -- misma clase de condición de carrera que el login (`useAuth`): `isModuleEnabled()` evaluaba con `tenant_modules` aún sin cargar (default `false`). `App.tsx` ahora espera a que `useTenantModules` termine de cargar antes de decidir cualquier redirect | Encontrado en auditoría UX/UI en vivo (navegación directa con Playwright), no reportado por el usuario -- confirmado real comparando contra el valor verdadero en la base (`purchasing: true`) |
| 2026-08-20    | Pulido UX/UI: pantalla de login envuelta en `Card` (antes flotaba sin contenedor), campo de foto de producto reemplazado por un botón "Subir/Cambiar foto" que dispara un input oculto (antes se veía el widget crudo del navegador "Choose File"), `autoComplete="off"` en los campos numéricos de venta a granel y efectivo recibido (cierra un glitch visual de autocompletado del navegador que mostraba un valor fantasma sin afectar el dato real enviado) | Auditoría UX/UI en vivo pedida por el usuario ("sistema de alto nivel"); cero deuda visual entre lo ya pulido (tickets, tarjetas, reportes) y estas pantallas que se habían quedado con el estilo por defecto |
| 2026-08-21    | Menú móvil (hamburguesa) ahora resalta en dorado la página actual, igual que el menú de escritorio -- antes no había ninguna indicación de en qué pantalla estabas parado al abrirlo en celular | Revisión de diseño del menú móvil pedida por el usuario; inconsistencia real entre escritorio (sí resaltaba) y móvil (no) |
| 2026-08-21    | Base real limpiada de toda la actividad de prueba generada durante esta sesión (6 ventas, 2 cajas, sus movimientos de inventario, y el producto "Tets"): quedan exactamente los 5 productos reales con su existencia inicial de 5kg cada uno, sin ninguna venta ni caja de prueba encima | Pedido explícito del usuario antes de empezar a operar de verdad |
| 2026-08-21    | Inventario: el campo "Cantidad" al registrar un movimiento ahora muestra la unidad del producto elegido ("Cantidad (KG)") -- antes era un número sin ninguna pista de si se esperaban kilos, gramos o piezas, con productos a granel eso es ambigüedad real (500 podía leerse como 500g o 500kg) | Encontrado revisando capturas reales de Inventario pedidas por el usuario ("inventario como ves") |
| 2026-08-21    | Diálogos largos con scroll ("Nuevo producto", "Nueva orden de compra") corregidos: el botón principal (Crear producto/orden) vivía dentro del área que se desplaza -- en una pantalla de laptop común (1280×720) había que bajar hasta el fondo para encontrarlo, y de paso había un desbordamiento horizontal real de ~12px (scrollbar reservando menos espacio del que de verdad ocupa en Windows). Ahora el encabezado y el pie (con el botón) quedan fijos, solo los campos del formulario se desplazan | Reportado por el usuario con capturas reales del formulario cortado; confirmado con medición real de scrollWidth/clientWidth antes de tocar el código, no solo por ojo |
| 2026-08-21    | Caja arranca cada venta con el método de pago por default ya seleccionado, en vez de vacío -- Del Campo cobra ~90% en efectivo. Genérico a propósito: `payment_methods.is_default` (un solo default por negocio, índice único) en vez de asumir "efectivo" en el código; un negocio que cobre más con tarjeta marca esa como su default y el mismo código funciona igual | El usuario pidió explícitamente NO hardcodear algo solo porque a Del Campo le conviene así -- se resolvió con un dato configurable por negocio, no con lógica fija |
| 2026-08-20    | Nombre del negocio sacado de texto hardcodeado a `tenant_settings` (fila única, editable solo por owner desde Configuración): usado en el ticket impreso (lo único que ve el cliente final, CLAUDE.md §7). Los 7 textos internos que decían "de Del Campo" en Usuarios/Compras/Clientes/Catálogo se volvieron genéricos -- el chrome interno siempre debe verse como Sunname ERP, nunca con el nombre del tenant | Encontrado en auditoría honesta sobre el objetivo multi-negocio (§1): "Del Campo" estaba escrito a mano en 8 archivos; para un negocio nuevo real, esas pantallas seguirían diciendo "Del Campo" sin este cambio |
| 2026-08-20    | Respaldo semanal (`.github/workflows/backup.yml`) verificado de extremo a extremo: se restauró un dump real completo en un Postgres local temporal (no se tocó producción) y los 6 productos, 5 ventas, 5 sale_items, 4 perfiles y 11 movimientos de inventario llegaron con valores exactos. Los únicos errores al restaurar fueron de esquemas internos de Supabase (`auth`/`storage`), esperados -- en una recuperación real se restaura contra un proyecto Supabase nuevo, que ya trae eso resuelto | Un respaldo nunca antes restaurado es una falsa sensación de seguridad; había que probar que el archivo generado sirve de verdad, no solo que el job de GitHub Actions corre sin error |
| 2026-08-20    | **Venta a granel por peso adelantada a Fase 1** (no esperó a Fase 3 como preveía el §4 original): `products` gana `sold_by_weight` + `price_per_100g` (tarifa de menudeo) junto al `price` ya existente (tarifa por kilo, desde 1kg); `create_sale` calcula el precio de cada línea del lado del servidor según ese quiebre de tarifa; Caja gana un cuadro de captura "por peso" (gramos de báscula) o "por monto" (pesos pedidos por el cliente, se resuelve el peso equivalente) — `apps/web/src/features/caja/GranelDialog.tsx`, `apps/web/src/lib/granel.ts` | El dueño de Del Campo aclaró que sus precios "por kilo" y "por 100g" no son dos productos fijos ni proporcionales entre sí (comprar el kilo completo siempre sale más barato por gramo): son dos tarifas del mismo producto, y el cliente puede pedir cualquier cantidad suelta (ej. 130g) o un monto en pesos (ej. "$20 de cacahuate"), no solo kilos enteros o bolsas de 100g fijas |
| 2026-08-20    | Ticket imprimible al cerrar una venta (`ReceiptDialog.tsx`, CSS de impresión en `index.css` que oculta todo excepto `#receipt-print-area`) y anulación de ventas (`void_sale` RPC, `owner`/`local_admin` only, revierte inventario con movimiento 'in' compensatorio sin borrar el rastro original) desde Reportes → Ventas recientes | Primeras dos brechas reales identificadas para operar el piloto: sin ticket no hay comprobante para el cliente, y sin anulación un error de captura del cajero no tenía corrección posible más que editar la base a mano |
| 2026-08-20    | `invite-user` asigna el rol real elegido en un segundo paso (`ctx.supabaseAdmin.from("profiles").update({role})`) después de crear al usuario; nueva migración `20260820203000_grant_service_role_privileges.sql` da a `service_role` los mismos GRANT de tabla que ya tenía `authenticated` (+ `alter default privileges`); `InviteUserDialog` ahora lee el mensaje real del cuerpo de la respuesta (`error.context.json()`) en vez del genérico "Edge Function returned a non-2xx status code" del SDK | El rol elegido en "Invitar usuario" nunca se aplicaba: `handle_new_user()` (parche de seguridad del mismo día) ignora a propósito el rol de los metadatos del signup y siempre crea `cashier`; al corregirlo se encontró en cadena que `service_role` tampoco tenía GRANT sobre las tablas creadas por migración (mismo problema documentado para `authenticated` ese mismo día) -- las funciones `security definer` nunca lo sufrieron porque corren con los privilegios de su dueño, no los de `service_role` |
| _(pendiente)_ | Límite de envío de correos de Supabase (servicio de email por default, no SMTP propio) se agota rápido en pruebas -- visto en vivo como "email rate limit exceeded" al invitar dos usuarios seguidos | Antes de invitar varios usuarios reales seguidos (ej. varios cajeros de una vez), considerar configurar un proveedor SMTP propio (Resend/Postmark/SendGrid) en el proyecto de Supabase; para un piloto de 2-3 personas invitadas de vez en cuando no es bloqueante |
| 2026-08-30    | Etiquetas con código de barras (`LabelPrintDialog.tsx`, librería `jsbarcode`, símbología CODE128): botón "Etiqueta" en cada fila/tarjeta de Catálogo (owner/local_admin). El SKU ya existente ("SKU / código de barras") se reutiliza como valor del código -- si el producto no tiene uno (el caso normal en venta a granel: una bolsa de chile no trae UPC de fábrica), el diálogo ofrece generar un código interno corto. Permite elegir cuántas copias imprimir; cada una se escala por CSS (`h-auto w-full` en el `<svg>`) para no desbordar su tarjeta -- sin eso, el ancho fijo que genera `jsbarcode` (~188px) se salía de tarjetas más angostas y las barras de dos etiquetas contiguas se encimaban en el papel, inutilizando el escaneo | Del Campo vende sobre todo a granel; su inventario no trae barcode de fábrica, así que la pieza que de verdad le faltaba al sistema no era leer códigos (eso ya existía en Caja) sino poder generar e imprimir los propios |
| 2026-08-30    | Corrección de bug real de impresión (afecta tanto el ticket de venta como la etiqueta nueva): imprimir desde una página de fondo alta (ej. Catálogo con su tabla larga) generaba una **página extra en blanco/duplicada** después de la primera, porque `visibility:hidden` no saca los elementos del flujo -- la altura real de la página de fondo seguía contando para la paginación de impresión aunque fuera invisible. Corregido ocultando `#root` por completo (`display:none`) durante impresión, verificado que el diálogo (`Dialog` de Base UI) se porta fuera de `#root` así que es seguro ocultarlo sin arriesgar lo que sí se imprime. De paso, el CSS de impresión compartido pasó de un id fijo (`#receipt-print-area`) a un atributo genérico (`[data-print-area]`) para que lo reutilice cualquier función de impresión futura sin duplicar reglas | Encontrado generando un PDF de impresión real con Playwright (`page.pdf()`), no solo una captura de pantalla con medios emulados -- ese método más superficial no lo hubiera detectado, ya reportaba "se ve bien" con el bug todavía presente |
| 2026-08-30    | Corrección de bug real en Caja: escanear un código de barras que ningún producto tiene registrado no daba ninguna señal -- el cajero no sabía si el lector falló o el producto no existe en el sistema. Ahora se muestra un aviso ("Código no reconocido") cuando el Enter no encuentra ni coincidencia exacta de SKU ni coincidencias parciales por nombre | Encontrado al revisar el código de escaneo ya existente en `SaleScreen.tsx` mientras se evaluaba qué le faltaba a "código de barras" como funcionalidad |
| 2026-08-30    | Corrección de bug real en `lib/errors.ts` (`reportError`, usado en las tres rutas que CLAUDE.md §14.2 marca como críticas: registrar venta, abrir caja, cerrar caja): un error de Supabase (`PostgrestError`/`AuthError`) es un objeto plano con `.message`, no una instancia de `Error`, así que `description` caía en `String(error)` y el cajero veía literalmente "[object Object]" en el toast en vez del motivo real | Encontrado probando en vivo una venta de un producto con precio $0.00 (dato real, aún sin precio cargado) -- el error real detrás (`sale_payments_amount_check`, el pago debe ser mayor a cero) solo se pudo ver después de corregir el bug de mensaje |
| 2026-08-30    | Auditoría completa (arquitectura/seguridad vía revisión de código + UX/UI vía capturas reales en escritorio y móvil de las 8 pantallas). Nada crítico: las tres vulnerabilidades ya corregidas antes siguen corregidas. Hallazgos reales que quedan abiertos, sin resolver todavía: cero pruebas automatizadas pese a que CLAUDE.md §11 las exige para caja/inventario/permisos (una migración del mismo día 2026-08-20 reintrodujo brevemente el bug del precio forjado en `create_sale`, sobreescrita después, prueba de que sin tests nada evita que se repita); `void_sale` no pasa por `reportError`/Sentry pese a ser una operación financiera sensible; `local_admin` puede invitar a otro `local_admin` (CLAUDE.md §6 solo le da alcance de "gestión de cajeros"); límites entre módulos se respetan por convención, no por algo que lo obligue en código | Pedido explícito del usuario ("audita porfavor... arquitectura sana") antes de seguir construyendo; usó un subagente de revisión de código en paralelo a la revisión visual propia para no sesgar el hallazgo |
| 2026-08-30    | Corrección de bug real en venta a granel "por monto" (`lib/granel.ts`, `granelWeightKgFromAmount`): el peso derivado de un monto en pesos (ej. "$50 de chile") se guardaba sin redondear (217.39g), así que el total mostrado en el carrito se calculaba sobre ese peso exacto ($50.00 exacto, por construcción matemática), pero `create_sale` guarda la cantidad como `numeric(12,3)` (gramos enteros, lo que de verdad se pesa) y cobraba sobre el peso ya redondeado (217g → $49.91) -- `create_sale` rechazaba la venta completa por el descuadre entre pago y subtotal. Ahora el peso se redondea a gramos enteros desde que se deriva del monto, antes de mostrar cualquier total, para que cliente y servidor calculen sobre el mismo número | Reportado por el usuario con una captura real del error en Caja ("Los pagos (50.00) no coinciden con el subtotal de la venta (49.91)") -- reproducido exacto (Chile Guajillo Herradura, $50, 217g) y verificado que ya no falla |
| 2026-08-30    | Catálogo real de Del Campo (78 productos, ~80% del total esperado) categorizado por completo: se agregaron 5 categorías nuevas (Granos y cereales, Leguminosas, Mariscos secos, Chocolates y endulzantes, Otros) junto a las 6 ya existentes, y se activó `sold_by_weight` en todos los productos que se venden sueltos por peso (la inmensa mayoría -- solo chocolate y piloncillo, que se venden por pieza, quedaron fuera). "Abas" se corrigió a "Habas" (errata de captura, confirmado con el usuario) | Pedido explícito del usuario; aplicado con un script directo contra la REST API de Supabase (login + PATCH por producto) en vez de uno por uno a mano en la UI -- 78 productos, no era viable por diálogo |
| 2026-08-30    | Corrección de bug real: en Catálogo (vista tabla) en mobile, la miniatura de foto del producto se aplastaba a ~2px de ancho en vez de mostrarse a 36×36px -- causa raíz de dos capas: (1) `<table>` tenía `w-full` (ancho forzado al 100% del contenedor) en vez de `min-w-full`, así que el layout automático de columnas nunca podía desbordar y hacer scroll horizontal como el wrapper `overflow-x-auto` ya permitía; (2) el reset base de Tailwind aplica `max-width: 100%` a todo `<img>`, y dentro de una celda de tabla eso crea una dependencia circular (el ancho de la columna depende del contenido, pero el contenido se limita al ancho ya calculado de la columna), colapsando la imagen casi a cero. Corregido con `min-w-full` en `table.tsx` (componente compartido, arregla scroll horizontal en cualquier tabla ancha) + `min-w-9` en el `<img>` de miniatura (`min-width` gana sobre `max-width` cuando compiten, rompe el colapso) | Reportado por el usuario con una captura real de mobile ("se ve una línea, ¿es normal o se rompió?") -- no era una imagen rota ni ausente (`imgComplete: true`, `naturalWidth: 1024`, URL válida), sino un bug de layout confirmado midiendo dimensiones reales antes y después del fix con Playwright, no solo por inspección visual |
| 2026-08-30    | Filtros en Catálogo (categoría, estado activo/inactivo, a granel sí/no, sin precio) e Inventario (categoría) -- deliberadamente NO se agregaron a Caja en esta pasada: es la pantalla de mayor uso y CLAUDE.md ya prioriza velocidad de cajero sobre navegación ahí; un filtro de categoría en Caja se evalúa aparte, con más cuidado de no ralentizar el caso común (el cajero ya sabe qué busca) | El usuario pidió evaluar si convenían filtros; se aceptó empezar por Catálogo/Inventario (bajo riesgo, alto valor con ~500 productos en camino) y dejar Caja para después |
| 2026-08-30    | Unidad de los 68 productos a granel que habían quedado en "Costal" (default accidental: `units_of_measure` se ordena alfabéticamente y "Costal" queda primero) corregida a KG, que es lo que de verdad representa su existencia. Además, el formulario de "Nuevo producto" ya no defaultea a la primera unidad alfabética -- prioriza "Pieza" (default neutral genérico) si existe | El usuario lo pidió explícitamente tras ver "0 COS" en Inventario para casi todo; pidió también corregir el default hacia adelante, no solo los datos ya cargados |
| 2026-08-30    | Correcciones de la auditoría del mismo día (arquitectura/seguridad): (1) `void_sale` ahora pasa por `reportError`/Sentry, igual que registrar venta y abrir/cerrar caja (CLAUDE.md §14.2); (2) `invite-user` ya bloquea que un `local_admin` invite a otro `local_admin`/`accountant`/`viewer` -- solo puede invitar cajeros, que es su alcance real (CLAUDE.md §6); el selector de rol en `InviteUserDialog` ya solo muestra "Cajero" cuando quien invita no es owner, para no ofrecer algo que el servidor de todos modos va a rechazar; (3) se quitó la aserción no-nula `data.user!` en `invite-user`, ahora responde un 500 limpio si la invitación no trae el id esperado; (4) primeras pruebas automatizadas del proyecto (`vitest`, `apps/web/src/lib/granel.test.ts`, corren en CI) -- cubren la lógica de precio a granel, incluyendo el bug de redondeo de gramos corregido hoy mismo, para que no se pueda repetir sin que CI lo atrape | El usuario pidió corregir todo lo encontrado en la auditoría; quedan fuera de esta pasada (por alcance, no por decisión de no hacerlas): pruebas de RLS/`create_sale` vía pgTAP, y una regla de lint que imponga los límites entre módulos -- ambas son trabajo más grande, quedan anotadas para después |
| 2026-08-30    | Revisión UX/UI de todo el sistema (capturas reales, escritorio y móvil, las 8 pantallas + diálogos clave). Tres hallazgos reales corregidos: (1) toda tabla ancha (Reportes, Inventario, Clientes...) se podía deslizar en mobile pero no había ninguna pista visual -- se veía como si las columnas de la derecha estuvieran cortadas; ahora el componente compartido `Table` (`components/ui/table.tsx`) muestra un degradado a los lados solo cuando de verdad hay más contenido por ver, y desaparece al llegar al final del scroll (arregla todas las pantallas con tabla de una sola vez). (2) En Caja, un producto con precio $0.00 (normal mientras se sigue cargando el catálogo real) se mostraba igual que uno con precio real -- ahora dice "Sin precio" en terracota, para que el cajero no lo confunda ni intente venderlo. (3) El placeholder del buscador de Caja se cortaba a la mitad en mobile por ser muy largo -- acortado | El usuario pidió una revisión de UX/UI de todo el sistema; los tres hallazgos se verificaron con capturas y medición real (scrollWidth/scrollLeft), no solo a ojo |
| 2026-08-31    | Miniatura de foto agregada a Inventario (mismo tamaño/patrón que Catálogo y Caja) -- decidido **NO** agregar lo mismo en Caja a una vista más grande, ni tarjetas con foto: CLAUDE.md ya decidió a propósito que ahí importa la velocidad del cajero, no la navegación visual, y la miniatura chica que ya existe resuelve el problema real (distinguir variantes parecidas) sin agregarle superficie visual a la pantalla de más uso | El usuario preguntó específicamente si convenía o no, pidiendo la opinión que más le sirviera al sistema y no la que le gustara a él -- se le dio una recomendación firme en ambos sentidos (sí en Inventario, no en Caja) con la razón de arquitectura/UX detrás, no una lista de opciones neutrales |
| 2026-08-31    | Filtro de categoría en Caja (`SaleScreen.tsx`), pendiente desde antes por diseñarse con cuidado. Es secundario a propósito: en `filterCategory: 'all'` (su default) el comportamiento de la búsqueda es idéntico al de siempre -- no le resta nada al camino rápido de escribir un nombre o escanear. Solo cuando se elige una categoría sin escribir texto, se muestran todos los productos activos de esa categoría (útil cuando el cliente pide "algo de especias" sin saber el nombre exacto); combinado con texto, filtra ambos a la vez. Verificado que el buscador y el selector no compitan por espacio en mobile (ambos se cortaban a la mitad antes del ajuste de `flex-wrap`) | El usuario pidió resolver esto primero, antes que las pruebas de RLS/`create_sale`; explícitamente pidió mantener la velocidad de Caja como prioridad al diseñarlo |
| 2026-08-31    | Corrección de bug real en venta a granel "por monto" (`lib/granel.ts`): con un producto todavía sin precio (`price_per_100g = 0`, normal mientras se carga el catálogo), la fórmula dividía entre cero -- `Infinity` en JS, y de ahí `NaN` en el total. Al mandarlo a `create_sale`, `JSON.stringify` convierte `Infinity`/`NaN` en `null` en silencio, y la base rechazaba la venta con "quantity no puede ser null", un error que no explicaba nada del problema real. `granelWeightKgFromAmount` ahora regresa 0 en vez de un valor no-finito (con una guarda `Number.isFinite` de cierre), y `GranelDialog` detecta un producto sin precio y muestra "Este producto todavía no tiene precio" en vez de dejar completar cualquiera de las dos pestañas -- ya no se llega hasta el error de cobro | Reportado por el usuario con una captura real del carrito mostrando "$NaN" e "Infinity g" en varias líneas de Frijol Pinto; reproducido exacto y verificado que el diálogo ya bloquea el flujo desde el origen, con una prueba de regresión agregada a `granel.test.ts` |
| 2026-08-31    | Primeras pruebas de RLS/`create_sale` (pgTAP, `supabase/tests/database/create_sale_security.sql`), corriendo en un job nuevo de CI (`db-tests`) que levanta una Supabase local con Docker y aplica todas las migraciones desde cero -- nunca contra el proyecto real. `deploy` ahora depende también de este job, no solo de `check`. Cubre los hallazgos ya cerrados de la auditoría de seguridad para que una migración futura no pueda reintroducirlos sin que CI lo atrape: `handle_new_user()` ignora el rol de los metadatos del signup, un cajero no puede insertar directo en `sales`/`inventory_movements`, `create_sale` rechaza pago/subtotal descuadrado, el precio siempre sale del catálogo (nunca de un `unit_price` forjado en el item), y una caja ajena queda rechazada | Docker no está instalado en la máquina de desarrollo actual, así que estas pruebas no se pudieron correr en local antes de subir -- decisión explícita del usuario de iterar contra CI (que sí trae Docker de por sí) en vez de instalar Docker primero o posponer esto |
| 2026-08-31    | Cobertura de RLS extendida a `sale_items`/`sale_payments` insert directo (mismo patrón que `sales`/`inventory_movements`, ya probado). Con esto queda cerrada, al menos en las tablas de venta, la recomendación de pruebas de RLS que dejó abierta la auditoría del mismo día | Confirmado en CI: 9/9 pruebas pasan |
| 2026-08-31    | Recomendación de lector de código de barras (§9): empezar con uno genérico USB 2D (Netum/Tera/Symcode, ~$350-800 MXN) en vez de uno profesional de entrada -- el catálogo apenas tiene 1-2 productos con código real asignado, no se justifica invertir más antes de saber cuánto uso real va a tener. Si el volumen lo justifica más adelante, subir a un Honeywell Voyager 1200g (~$1,189-1,900 MXN, IP42, 5 años de garantía) | El usuario pidió cerrar este pendiente de §9; se investigaron precios reales vigentes en México (Mercado Libre, PCEL, Cyberpuerta, Amazon México) en vez de dar cifras de memoria |
| 2026-08-31    | Segunda auditoría (agente de revisión de código en paralelo + revisión visual propia con capturas reales). Nada crítico, nada regresado de la primera auditoría. Un hallazgo ALTO real corregido: `create_sale` nunca validaba que la cantidad de un item fuera mayor a cero -- como es security definer, un cajero podía llamarla directo (sin pasar por GranelDialog, que sí bloquea 0 en la UI) con cantidad 0 (tumbaba la función con "division by zero" sin manejar) o negativa (colaba un subtotal negativo que, con un pago negativo a juego, pasaba la validación de pago=subtotal). Corregido con una guarda al inicio del loop de items, cubierta con 2 pruebas pgTAP nuevas. De paso se agregó cobertura de `void_sale` (0 pruebas antes de hoy pese a ser tan crítica como `create_sale`): rechazo a un cajero, reversión de inventario correcta, rechazo de doble anulación -- 15/15 pruebas en total ahora. Dos hallazgos MEDIOS también corregidos: la sombra de scroll de `Table` solo se recalculaba en resize de ventana, no cuando un filtro cambiaba el contenido (ahora observa el `<table>` además del wrapper); y una aserción no-nula frágil en `LabelPrintDialog` (cambiada por una const local que TS sí puede propagar) | El agente identificó el hallazgo del cajero saltándose la UI llamando `create_sale` directo -- exactamente el tipo de vector que la ronda de correcciones del 30 de agosto se propuso cerrar, y que el primer set de pruebas pgTAP no cubría |
| 2026-08-31    | Catálogo real de Del Campo (había crecido de 78 a 164 productos) revisado por completo: ortografía (acentos faltantes -- "Maiz"→"Maíz", "Camaron"→"Camarón", "Chia"→"Chía" --, errores de captura -- "Palollo Dental"→"Palillo Dental", "Brrilito"→"Barrilito", "Papel Destraza"→"Papel De Estraza", "Pelicula Aderible"→"Película Adherible" --, y concordancia de género -- "Sal Molido"→"Sal Molida"), 89 productos categorizados/marcados a granel/con unidad asignada, 3 categorías nuevas creadas (Aceites y vinagres, Pulpas y concentrados, Empaque e insumos -- mismo criterio de siempre: solo con productos reales ya cargados, no especulativas), y un bug real de datos corregido de paso: 3 productos no-granel (Chocolate Abuelita, 2 Piloncillos) habían quedado en unidad "Costal" por el mismo bug de default alfabético ya corregido el 2026-08-30 para productos a granel -- ese fix no los cubrió porque solo buscó productos con `sold_by_weight=true`. 6 nombres quedaron sin tocar por incertidumbre real (Chile Ayanero, Palo Elotero, Mil De Zinacantepec, Soya Fin Texturizada, Arroz Samam, y un posible duplicado Haba Pelado Mediano/Habas Peladas Medianas) -- se categorizaron igual, pero el nombre no se cambió sin confirmación del dueño | Pedido explícito del usuario ("revisa todos los productos... ortografía, categorías, a granel, unidad"); aplicado con el mismo patrón de script REST ya usado el 30 de agosto, no uno por uno a mano -- 89 cambios no era viable por diálogo |
| 2026-08-31    | Buscador con ícono y botón de limpiar (`components/ui/search-input.tsx`, nuevo) reemplaza el `Input` simple en los tres buscadores de producto (Catálogo, Inventario, Caja) -- antes, limpiar una búsqueda para hacer la siguiente exigía borrar letra por letra. El botón limpia y regresa el foco al campo en el mismo clic. De paso, las tres búsquedas (más el escaneo de SKU en Caja) pasan a comparar con `normalizeSearch()` (`lib/text.ts`, nuevo -- quita acentos vía `.normalize('NFD')` antes de comparar) en vez de solo `.toLowerCase()`: buscar "curcuma" ahora sí encuentra "Cúrcuma" | El usuario lo reportó en vivo, dando de alta muchos productos el mismo día: "tengo que borrar letra por letra" y "no encontraba algunos productos... por acentos". Verificado con Playwright que "curcuma" sin acento encuentra "Cúrcuma", que el botón de limpiar aparece/limpia/regresa el foco en los tres buscadores, y que el escaneo de código de barras en Caja (que depende del mismo campo) sigue funcionando |
| 2026-08-31    | Resueltos los 6 nombres de producto que quedaron pendientes de confirmar en la pasada de catálogo: "Chile Ayanero"→"Chile Habanero", "Mil De Zinacantepec"→"Miel De Zinacantepec", "Soya Fin Texturizada"→"Soya Fina Texturizada"; "Palo Elotero" y "Arroz Samam" se confirmaron correctos tal cual (el primero es real: un palo que se coloca en la base de un elote para poder sostenerlo; el segundo es una marca real de arroz); "Haba Pelado Mediano" se desactivó por ser el mismo producto que "Habas Peladas Medianas" (que ya tenía SKU asignado) | El dueño de Del Campo confirmó cada uno directamente; "Haba Pelado Mediano" quedó desactivado en vez de borrado porque el sistema no permite borrar productos con historial potencial (ver §11 -- ninguna tabla de ventas/inventario/compras permite borrado en cascada de un producto a propósito, para no perder el rastro de operaciones ya hechas) |
| 2026-08-31    | Revisión proactiva de UX/UI de todo el sistema (capturas reales con Playwright, escritorio 1440px + tablet 768px + mobile 390px, las 8 pantallas + diálogos clave), pedida por el usuario tras preguntar si Claude también puede encontrar mejoras sin que él las reporte primero. Dos bugs reales encontrados y corregidos: (1) en Catálogo mobile, el buscador compartía fila sin `flex-wrap` con los botones de vista tabla/tarjetas -- a diferencia de la fila de filtros justo debajo, que sí envolvía -- y ninguno de los dos buscadores (Catálogo/Inventario) tenía un ancho mínimo, así que el navegador los encogía en vez de bajarlos de línea, repitiendo el mismo truncado de placeholder que ya se había corregido en Caja el 30 de agosto ("Buscar producto por nombre o" cortado, sin mostrar "SKU…"); corregido con `flex-wrap` + `min-w-[300px]`/`min-w-[200px]` en cada buscador, verificado que a 1440px y 768px no cambia nada y a 390px el buscador ahora baja a su propia línea completa. (2) Ni Catálogo ni Inventario mostraban cuántas páginas había en total, solo "Anterior/Siguiente" -- con 164 productos (7 páginas) y un catálogo real camino a varios cientos, no había forma de saber qué tan lejos se estaba sin contar clics; se agregó "· página X de Y" al mismo componente compartido `PaginationControls`, así que la mejora aplica a cualquier tabla paginada del sistema sin tocarla una por una. Un hallazgo real que se dejó **sin corregir**, reportado al usuario para que decida: en Configuración, el interruptor de "Facturación" es tan interactivo como los de CRM/Compras (que sí muestran/ocultan funcionalidad real) pero no tiene ningún efecto visible -- no existe ruta ni módulo de facturación construido todavía -- lo cual puede confundir al dueño pensando que activó o desactivó algo | El usuario preguntó "¿tú puedes encontrar mejoras en usabilidad?" tras notar que las dos mejoras anteriores del buscador solo se descubrieron porque él estaba trabajando; se le explicó la diferencia real (una revisión con Playwright no reproduce horas de uso real) y se ofreció hacer una pasada de todos modos, que confirmó que sí vale la pena hacerla periódicamente -- encontró dos bugs reales que ni la revisión visual anterior del mismo día había detectado, porque solo se habían probado con pocos productos, no con el catálogo real de 164 |
| 2026-08-31    | Resuelto el hallazgo del interruptor de Facturación: en vez de un `Switch` interactivo, ahora muestra una insignia "Próximamente" (sin interacción) -- se mantiene visible en la lista por transparencia de roadmap, pero ya no aparenta controlar algo que no existe todavía | El usuario eligió "deshabilitalo con proximamente" entre las dos opciones ofrecidas (deshabilitar vs. quitar de la pantalla) |